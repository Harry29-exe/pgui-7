import React from 'react';
import {center} from "./style";

const PrivatePage = () => {

    return (
        <div style={center}>
            <h1>This is page available only for logged users</h1>
        </div>
    );
};

export default PrivatePage;